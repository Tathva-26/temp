/**
 * Static images that used to be bundled under `public/` are now served from
 * this CDN instead, keeping the same path structure (e.g. `public/images/x.png`
 * becomes `${CDN_BASE_URL}/images/x.png`).
 */
export const CDN_BASE_URL = 'https://cdn-next.tathva.org'
